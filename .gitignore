# Raw corridor captures (732 MB). The pipeline reads these to build
# data/derived; the running app only needs the derived artefacts.
data/Images/

# Generated thumbnails
data/derived/thumbs/

__pycache__/
*.py[cod]
.venv/
venv/
.env
.DS_Store

# Reference screenshots dropped in the working directory
/image*.png
/ChatGPT Image*.png

# Storefront photographs. Captured under written permission from mall
# management for this project; that permission does not extend to
# redistribution, so only the derived vectors are published.
data/dataset/gallery/
data/dataset/query/
